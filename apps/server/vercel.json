{
  "buildCommand": "cd ../.. && yarn install && npx turbo run build --filter=server",
  "installCommand": "",
  "framework": "express",
  "outputDirectory": "dist",
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  }
}
